import { RemovewhitespacesPipe } from './removeWhiteSpaces.pipe';

describe('RemovewhitespacesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemovewhitespacesPipe();
    expect(pipe).toBeTruthy();
  });
});
